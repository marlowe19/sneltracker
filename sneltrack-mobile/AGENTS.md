# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 deliberately (not latest) so the app runs in the current
App Store release of Expo Go, which only supports SDK 54 clients as of this
writing. Do not bump the `expo` package without first checking the Expo Go
App Store version supports the target SDK.
